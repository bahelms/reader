# defmodule Reader.HomeView do
#   use Reader.Web, :view

#   def titleize(string) do
#     string
#       |> String.split("_")
#       |> Enum.map(fn(word) -> String.capitalize(word) end)
#       |> Enum.join(" ")
#   end
# end

